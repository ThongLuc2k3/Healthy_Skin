import { useState } from 'react'
import { Navigate,useNavigate } from 'react-router-dom'
import { LockKeyhole,LoaderCircle,ShieldCheck } from 'lucide-react'
import LogoIcon from '../components/LogoIcon'
import { useAuth } from '../context/AuthContext'
export default function AdminLoginPage(){const {user,loginAdmin}=useAuth(),navigate=useNavigate(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');if(['admin','super_admin'].includes(user?.role))return <Navigate to="/admin" replace/>;async function submit(event){event.preventDefault();setBusy(true);setError('');try{await loginAdmin(email,password);navigate('/admin',{replace:true})}catch(e){setError(e.message)}finally{setBusy(false)}}return <main className="grid min-h-screen place-items-center bg-[#0b1730] p-5"><section className="w-full max-w-md rounded-3xl bg-white p-7 shadow-2xl md:p-9"><LogoIcon/><div className="mt-8 flex h-14 w-14 items-center justify-center rounded-2xl bg-blue-50 text-[#2D5BFF]"><LockKeyhole/></div><h1 className="mt-5 text-3xl font-black">Đăng nhập quản trị</h1><p className="mt-2 text-sm leading-6 text-slate-500">Phiên quản trị không được ghi nhớ. Tải lại hoặc đóng trang sẽ phải đăng nhập lại.</p><form onSubmit={submit} autoComplete="off" className="mt-7"><label>Email quản trị<input autoFocus required type="email" autoComplete="off" value={email} onChange={e=>setEmail(e.target.value)} placeholder="admin@example.com"/></label><label className="mt-4">Mật khẩu<input required type="password" autoComplete="off" value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></label>{error&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-600">{error}</p>}<button disabled={busy} className="btn-primary mt-6 w-full justify-center">{busy?<><LoaderCircle className="animate-spin" size={18}/>Đang xác thực</>:<><ShieldCheck size={18}/>Đăng nhập an toàn</>}</button></form></section></main>}
